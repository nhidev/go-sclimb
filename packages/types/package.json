{
  "name": "@24dot7/types",
  "private": true,
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "type": "module",
  "scripts": {
    "type-check": "tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "lint": "eslint \"src/**/*.ts*\"",
    "lint:fix": "pnpm lint --fix"
  },
  "devDependencies": {
    "@24dot7/tsconfig": "workspace:*"
  }
}
